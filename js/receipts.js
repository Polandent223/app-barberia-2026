App.renderReceipts = function(){
  const f=App.val("receiptFrom")||"0000-01-01",t=App.val("receiptTo")||"9999-12-31";
  App.byId("receiptList").innerHTML=App.db.sales.filter(s=>s.date>=f&&s.date<=t).slice().reverse().map(s=>`<div class="receipt"><div class="panel-head"><div><strong>Recibo #${s.number}</strong><div class="muted">${s.date} ${s.time}</div></div><span class="currency-chip">${s.currency}</span></div><div class="row"><strong>Cliente</strong><strong>${s.clientName}</strong></div><div class="row"><strong>Barbero</strong><strong>${s.barberName}</strong></div><div class="receipt-items">${s.items.map(i=>`<div class="row"><div><strong>${i.name}</strong><small>${i.qty} × ${s.currency}${i.unit.toFixed(2)}</small></div><strong>${s.currency}${i.total.toFixed(2)}</strong></div>`).join("")}</div><div class="row"><strong>Total</strong><strong>${s.currency}${s.total.toFixed(2)}</strong><div class="manage-actions"><button class="btn secondary" onclick="App.printReceipt('${s.id}')">Imprimir</button><button class="btn danger" onclick="App.requestDelete('sale','${s.id}')">${App.deleteButtonLabel()}</button></div></div>`).join("")||'<div class="muted">Sin recibos.</div>';
};
