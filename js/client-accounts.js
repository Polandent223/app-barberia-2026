/* SAMBRIX · Cuenta del cliente, sesión local de compatibilidad y prevención de duplicados */
(function(){
 const A=window.App;if(!A)return;
 const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
 const normPhone=v=>String(v||'').replace(/\D/g,'');
 const businessId=()=>String(A.db?.meta?.businessId||window.SaaS?.currentBusiness?.()?.id||'local');
 const sessionKey=()=>`sambrix_client_session_${businessId()}`;
 const cloudClientMode=()=>{const q=new URLSearchParams(location.search);return q.get("cliente")==="app"&&!!q.get("business")};
 A.ensureClientAccounts=function(){A.db.clientAccounts=A.db.clientAccounts||[];return A.db.clientAccounts};
 A.clientAccountByPhone=function(phone){const p=normPhone(phone);return A.ensureClientAccounts().find(x=>normPhone(x.phone)===p)};
 A.clientAccountById=function(id){return A.ensureClientAccounts().find(x=>x.id===id)};
 A.currentClientAccount=function(){if(cloudClientMode())return null;const id=localStorage.getItem(sessionKey());const acc=A.clientAccountById(id);if(!acc&&id)localStorage.removeItem(sessionKey());return acc||null};
 A.currentClient=function(){const acc=A.currentClientAccount();return acc?A.db.clients.find(x=>x.id===acc.clientId)||null:null};A.clientLoggedIn=()=>!!A.currentClient();
 A.clientPinHash=function(pin,salt){let h=2166136261>>>0,str=`${salt}|${pin}|SAMBRIX_CLIENT`;for(let r=0;r<240;r++)for(let i=0;i<str.length;i++){h^=str.charCodeAt(i);h=Math.imul(h,16777619)>>>0;}return h.toString(16).padStart(8,'0')};A.newClientSalt=()=>A.uid()+A.uid();
 A.ensureClientAuthUI=function(){const shell=document.querySelector('#clientApp .client-shell');if(!shell)return;let auth=document.getElementById('clientAuth2031');if(!auth){auth=document.createElement('section');auth.id='clientAuth2031';auth.className='client-auth-2031';document.querySelector('#clientApp .client-top')?.insertAdjacentElement('afterend',auth)}let badge=document.getElementById('clientAccountBadge2031');if(!badge){badge=document.createElement('div');badge.id='clientAccountBadge2031';badge.className='client-account-badge hidden';document.querySelector('#clientApp .client-top')?.appendChild(badge)}A.renderClientAuthUI()};
 A.renderClientAuthUI=function(mode){const auth=document.getElementById('clientAuth2031'),badge=document.getElementById('clientAccountBadge2031');if(!auth)return;const acc=A.currentClientAccount(),c=A.currentClient(),pages=[...document.querySelectorAll('#clientApp .client-page')],nav=document.querySelector('#clientApp .client-bottom');if(acc&&c){auth.classList.add('hidden');pages.forEach(p=>p.classList.remove('client-auth-locked'));nav?.classList.remove('hidden');if(badge){badge.classList.remove('hidden');badge.innerHTML=`<div><strong>${esc(c.name)}</strong><small>${esc(c.phone||acc.phone)}</small></div><button class="btn secondary" type="button" onclick="App.clientLogout2031()">Salir</button>`}A.syncClientAccountFields();return}badge?.classList.add('hidden');nav?.classList.add('hidden');pages.forEach(p=>p.classList.add('client-auth-locked'));const register=mode==='register';auth.classList.remove('hidden');auth.innerHTML=`<div class="client-auth-card"><span class="tag">CUENTA DEL CLIENTE</span><h2>${register?'Crear mi cuenta':'Ingresar'}</h2><p class="muted">${register?'Regístrate una sola vez. Tus reservas quedarán asociadas a tu perfil.':'Ingresa para reservar y administrar tus citas.'}</p>${register?`<div class="form-grid"><label>Nombre<input id="clientRegName2031"></label><label>WhatsApp<input id="clientRegPhone2031" inputmode="tel"></label><label>PIN<input id="clientRegPin2031" type="password" inputmode="numeric" maxlength="8" placeholder="6 a 8 dígitos"></label><label>Repetir PIN<input id="clientRegPin22031" type="password" inputmode="numeric" maxlength="8"></label></div><div class="actions"><button class="btn primary" onclick="App.clientRegister2031()">Crear cuenta</button><button class="btn secondary" onclick="App.renderClientAuthUI('login')">Ya tengo cuenta</button></div>`:`<div class="form-grid"><label>WhatsApp<input id="clientLoginPhone2031" inputmode="tel"></label><label>PIN<input id="clientLoginPin2031" type="password" inputmode="numeric" maxlength="8"></label></div><div class="actions"><button class="btn primary" onclick="App.clientLogin2031()">Ingresar</button><button class="btn secondary" onclick="App.renderClientAuthUI('register')">Crear cuenta</button></div>`}</div>`};
 A.clientRegister2031=function(){if(cloudClientMode())return A.toast('Conectando cuenta segura...');const name=A.val('clientRegName2031').trim(),phoneRaw=A.val('clientRegPhone2031'),phone=normPhone(phoneRaw),pin=A.val('clientRegPin2031'),pin2=A.val('clientRegPin22031');if(!name||phone.length<7)return A.toast('Escribe nombre y WhatsApp válido');if(!/^\d{6,8}$/.test(pin))return A.toast('El PIN debe tener entre 6 y 8 números');if(pin!==pin2)return A.toast('Los PIN no coinciden');if(A.clientAccountByPhone(phone))return A.toast('Ese WhatsApp ya tiene cuenta');let c=A.db.clients.find(x=>normPhone(x.phone)===phone);if(!c){c={id:A.uid(),name,phone:phoneRaw.trim(),birthday:'',frequency:20,style:'',points:0,visits:0,lastVisit:''};A.db.clients.push(c)}const salt=A.newClientSalt(),acc={id:A.uid(),clientId:c.id,phone:phoneRaw.trim(),salt,pinHash:A.clientPinHash(pin,salt),createdAt:new Date().toISOString(),status:'Activo'};A.ensureClientAccounts().push(acc);localStorage.setItem(sessionKey(),acc.id);A.persist();A.ensureClientAuthUI();A.clientGo('clientHome');A.toast('Cuenta creada')};
 A.clientLogin2031=function(){if(cloudClientMode())return A.toast('Conectando cuenta segura...');const acc=A.clientAccountByPhone(A.val('clientLoginPhone2031')),pin=A.val('clientLoginPin2031');if(!acc)return A.toast('No existe una cuenta con ese WhatsApp');if(acc.status==='Bloqueado')return A.toast('Esta cuenta está bloqueada');if(A.clientPinHash(pin,acc.salt)!==acc.pinHash)return A.toast('PIN incorrecto');if(!A.db.clients.some(x=>x.id===acc.clientId))return A.toast('Cuenta sin cliente válido');acc.lastLoginAt=new Date().toISOString();localStorage.setItem(sessionKey(),acc.id);localStorage.setItem(A.KEY,JSON.stringify(A.db));A.ensureClientAuthUI();A.clientGo('clientHome');A.toast('Sesión iniciada')};
 A.clientLogout2031=function(){localStorage.removeItem(sessionKey());A.clientSelection={serviceId:'',barberId:'',time:''};A.renderClientAuthUI('login');A.toast('Sesión cerrada')};
 A.syncClientAccountFields=function(){const c=A.currentClient();if(!c)return;[['clientBookName',c.name],['clientBookPhone',c.phone],['clientLookupPhone',c.phone],['clientProfilePhone',c.phone],['clientHistoryPhone',c.phone]].forEach(([id,v])=>{const el=document.getElementById(id);if(el){el.value=v||'';el.readOnly=true}})};
 const prevOpen=A.openClientApp;A.openClientApp=function(){prevOpen?.();A.ensureClientAuthUI();if(!A.clientLoggedIn()){document.querySelectorAll('#clientApp .client-page').forEach(p=>p.classList.remove('active'));A.renderClientAuthUI('login')}else{A.syncClientAccountFields();A.lookupClientAppointments();A.lookupClientProfile();A.lookupClientHistory?.()}};
 const prevRender=A.renderClientApp;A.renderClientApp=function(){const r=prevRender?.();A.ensureClientAccounts();A.ensureClientAuthUI();return r};const prevGo=A.clientGo;A.clientGo=function(page){if(!A.clientLoggedIn()){A.renderClientAuthUI('login');return A.toast('Ingresa o crea tu cuenta para continuar')}const r=prevGo?.(page);A.syncClientAccountFields();if(page==='clientAppointments')A.lookupClientAppointments();if(page==='clientProfile')A.lookupClientProfile();if(page==='clientHistory')A.lookupClientHistory?.();return r};
 A.lookupClientProfile=function(){const c=A.currentClient();if(!c)return;const progress=Math.min(100,Number(c.points||0));A.byId('clientProfileData').innerHTML=`<div class="loyalty-card"><span>MI CUENTA SAMBRIX</span><h2 style="color:#fff;margin:8px 0">${esc(c.name)}</h2><div>${esc(c.phone||'')} · <strong>${c.points||0} puntos</strong> · ${c.visits||0} visitas</div><div class="loyalty-progress"><span style="width:${progress}%"></span></div></div>`};
 A.applyClientAccountPrivateUI=function(){['clientLookupPhone','clientProfilePhone','clientHistoryPhone'].forEach(id=>{const input=document.getElementById(id),grid=input?.closest('.form-grid');if(grid)grid.classList.add('client-account-search-hidden')})};document.addEventListener('DOMContentLoaded',()=>{A.ensureClientAccounts();A.ensureClientAuthUI();A.applyClientAccountPrivateUI();A.syncClientAccountFields()});
})();