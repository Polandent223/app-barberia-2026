/* SAMBRIX · Confirmación administrativa de reservas */
(function(){
 const A=window.App,S=window.SaaS;if(!A)return;const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),statusLabel=s=>s==='Pendiente'?'Pendiente de confirmación':s;
 const canSchedule=()=>!S||S.canWriteDomain?.('schedule')!==false;
 A.confirmAppointment2032=function(id){if(!canSchedule())return A.toast('No tienes permiso para confirmar citas');const a=A.db.appointments.find(x=>x.id===id);if(!a||a.status!=='Pendiente')return;a.status='Confirmada';a.confirmedAt=new Date().toISOString();a.confirmedBy=A.currentUser?.()?.name||'Administrador';A.persist();A.renderAppointments?.();A.lookupClientAppointments?.();A.toast('Cita confirmada')};
 A.rejectAppointment2032=function(id){if(!canSchedule())return A.toast('No tienes permiso para rechazar citas');const a=A.db.appointments.find(x=>x.id===id);if(!a||a.status!=='Pendiente')return;a.status='Rechazada';a.rejectedAt=new Date().toISOString();a.rejectedBy=A.currentUser?.()?.name||'Administrador';A.persist();A.renderAppointments?.();A.lookupClientAppointments?.();A.toast('Solicitud de cita rechazada')};
 A.lookupClientAppointments=function(){const c=A.currentClient?.(),root=A.byId('clientAppointmentsList');if(!root)return;if(!c){root.innerHTML='<div class="muted">Inicia sesión para ver tus citas.</div>';return}const data=A.db.appointments.filter(a=>a.clientId===c.id).sort((a,b)=>(b.date+b.time).localeCompare(a.date+a.time));root.innerHTML=data.map(a=>{const req=(A.db.clientRequests||[]).filter(r=>r.appointmentId===a.id&&r.type==='reschedule').sort((x,y)=>(y.createdAt||'').localeCompare(x.createdAt||''))[0],canManage=['Pendiente','Confirmada'].includes(a.status)&&req?.status!=='Pendiente';return `<div class="row"><div><strong>${esc(a.date)} ${esc(a.time)}</strong><small>${esc(A.serviceName(a.serviceId))} · ${esc(A.barberName(a.barberId))} · <strong>${esc(statusLabel(a.status))}</strong></small>${a.status==='Pendiente'?'<div class="muted">El negocio todavía debe confirmar esta cita.</div>':''}</div>${canManage?`<div class="request-actions"><button class="btn secondary" onclick="App.openReschedule('${a.id}')">Reprogramar</button><button class="btn danger" onclick="App.requestAppointmentChange('${a.id}','cancel')">Cancelar</button></div>`:''}</div>`}).join('')||'<div class="muted">No tienes citas registradas.</div>'};
 const previousRender=A.renderAppointments;A.renderAppointments=function(){previousRender?.();const root=A.byId('appointmentList');if(!root)return;const q=A.filters.appointments||'',data=A.db.appointments.slice().reverse().filter(a=>!q||A.clientName(a.clientId).toLowerCase().includes(q)||A.barberName(a.barberId).toLowerCase().includes(q)||A.serviceName(a.serviceId).toLowerCase().includes(q));root.innerHTML=data.map(a=>`<div class="row"><div><strong>${esc(a.date)} ${esc(a.time)} · ${esc(A.clientName(a.clientId))}</strong><small>${esc(A.serviceName(a.serviceId))} · ${esc(A.barberName(a.barberId))} · <strong>${esc(statusLabel(a.status))}</strong></small></div><div class="manage-actions">${a.status==='Pendiente'?`<button class="btn primary" onclick="App.confirmAppointment2032('${a.id}')">Confirmar</button><button class="btn danger" onclick="App.rejectAppointment2032('${a.id}')">Rechazar</button>`:''}${a.status==='Confirmada'?`<button class="btn secondary" onclick="App.finishAppointment('${a.id}')">Finalizar</button>`:''}${a.status!=='Finalizada'?`<button class="btn edit" onclick="App.editAppointment('${a.id}')">Editar</button>`:''}<button class="btn danger" onclick="App.deleteAppointment('${a.id}')">${A.deleteButtonLabel()}</button></div></div>`).join('')||'<div class="muted">Sin citas.</div>';A.renderAgendaRequests?.();S?.applyBusinessTerminology?.()};setTimeout(()=>{A.renderAppointments?.();A.lookupClientAppointments?.()},120);
})();